<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.istiqomahstore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activity.Activity.PaymentActivity">

        </activity>
        <activity android:name=".activity.Activity.EditProfileActivity">

        </activity>
        <activity android:name=".activity.Activity.SuccessForgotActivity" />
        <activity android:name=".activity.Activity.ForgotPasswordActivity" />
        <activity android:name=".activity.Activity.RegisterActivity">
        </activity>
        <activity android:name=".activity.Activity.LoginActivity">

        </activity>
        <activity android:name=".activity.Activity.SplashScreenActivity">
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>